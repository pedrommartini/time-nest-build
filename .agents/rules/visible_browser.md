# Visible Browser Rule

- Always use the dedicated "Antigravity" Chrome profile (`--profile-directory="Profile 1"`).
- Launch all browser interactions directly inside the user's visible "Antigravity" Chrome window.
- Never close Chrome tabs or exit the Chrome browser automatically.
